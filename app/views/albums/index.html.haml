%h1 Listing albums

%table
  %tr
    %th Title
    %th Subtitle
    %th Description
    %th Issue date
    %th{ :colspan => 3} Actions

  - @albums.each do |album|
    %tr
      %td= album.title
      %td= album.subtitle
      %td= album.description
      %td= album.issue_date
      - if current_user
        %td= link_to 'Show', user_album_path(current_user, album)
        %td= link_to 'Edit', edit_user_album_path(current_user, album)
        %td= link_to 'Destroy', user_album_path(current_user, album), :confirm => 'Are you sure?', :method => :delete


%br

= link_to 'New Album', new_user_album_path(current_user) if current_user
